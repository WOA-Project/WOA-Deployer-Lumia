using System.Threading.Tasks;
using Xunit;

namespace Deployer.Lumia.Tests
{
    public class LumiaDiskLayoutPreparerReloadedTests
    {
        [Fact]
        [Trait("Category", "Real")]
        public async Task Prepare()
        {
            //var preparer = new LumiaDiskLayoutPreparer(new FileSystemOperations(), );
            //var api = new DiskApi();
            //var disk = await api.GetDisk(3);
            //await preparer.Prepare(disk);
        }
    }
}
